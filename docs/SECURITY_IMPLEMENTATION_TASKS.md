# Verigence Security — Implementation Tasks

**Status:** ACTIVE  
**Created:** 2026-08-15  
**Updated:** 2026-08-16  
**Design:** `docs/SECURITY_CROSS_MODULE_AUTH_DESIGN_v1.0.md` / VSEC-SD-INT-001 v1.1

This file is the operational progress tracker for Security work introduced by approved authentication, authorization and cross-module integration decisions. It does not mark design-only work as implemented.

## Current position

**Tasks:** 4  
**COMPLETE:** 2  
**IN PROGRESS:** 0  
**BLOCKED:** 0  
**NOT STARTED:** 2

The next implementation task is `SEC-AUTH-001`. After it is verified, `SEC-DEPLOY-001` deploys the resulting Security OAuth/authentication service to DEV and closes the currently observed `/oauth/token` 404 deployment gap.

| ID | Task | Status | Deliverable | Acceptance | Dependency / note |
|---|---|---|---|---|---|
| SEC-INT-001 | Implement platform permissions, service integration and delegated OAuth token exchange | COMPLETE | Security capability for (a) Security user JWTs carrying the Tenant's effective cross-module platform `permissions[]`, (b) Tenant-scoped short-lived `SERVICE` tokens for approved module-owned/admin/background execution and (c) OAuth delegated token exchange/on-behalf-of tokens narrowed to user + integration + requested downstream authority | `src/verigence_security/settings.py`, `tokens.py` and `app.py` implement configuration-driven platform permission bundles, RS256/JWKS issuance, `client_credentials` service tokens and OAuth token exchange with `act.sub` caller attribution; `tests/test_oauth.py` verifies cross-module user permissions, service least privilege, delegated user/Tenant preservation, permission intersection denial, invalid-client denial and JWKS/DI-contract validation; GitHub Actions run `31884091488` passed Ruff and all tests on commit `4692aa6cf51109d9f39416f638ef258a2fcf63ec` | Required by Audit Core `G-01`; downstream token semantics remain approved. Current Railway DEV deployment does not yet expose this implementation and is tracked separately in `SEC-DEPLOY-001`. |
| SEC-RBAC-001 | Implement default PC/TL/PM/CRM templates and Tenant-customizable role management | COMPLETE | Security-owned approved default cross-module role catalogue; persistent platform/Tenant role templates; Tenant onboarding seed; Super Admin/Tenant Admin management authorization; effective token resolution from Tenant role configuration | `config/default_role_templates.json` and `src/verigence_security/role_templates.py` implement the approved four defaults and guarded registered-permission catalogue; `database/0001_role_templates.sql` persists platform/Tenant templates plus append-only change audit; `src/verigence_security/app.py` exposes Tenant bootstrap/read/update and Super Admin platform-default read/update APIs; `TokenService` resolves Tenant role templates into newly issued `permissions[]`; `tests/test_role_templates.py` and `tests/test_oauth.py` prove PC capture/upload without verification-write, TL/PM verification, CRM DI read-only, Tenant Admin own-Tenant scope, Super Admin cross-Tenant/platform changes, no silent rewrite of existing Tenants, forbidden/unknown/admin permission rejection, token update after role change, change audit, and PostgreSQL persistence across store recreation; GitHub Actions run `31884922331` passed PostgreSQL 16 setup, Ruff and all tests on commit `d1ea624f81f8879f3b597d25b2bb27dc5355a94e` | Design: `docs/SECURITY_DEFAULT_ROLE_TEMPLATES_v1.0.md`; machine defaults: `config/default_role_templates.json`; Audit Core source: `VAC-SD-RBAC-001` + `AUDIT_CORE_SECURITY_CATALOG_v2.1.json`. |
| SEC-AUTH-001 | Implement Security-owned interactive authentication, session and OAuth authorization-code flow | NOT STARTED | Security-owned `/auth/login`, `/auth/callback`, `/auth/logout`, `/session`, `/oauth/authorize`, and `authorization_code` handling at `/oauth/token`; upstream IdP is behind Security; modules act only as registered OAuth clients; Security resolves Verigence user/Tenant/roles and issues the authoritative USER token | Minimum risk-based tests must prove: unauthenticated authorization initiates Security-owned login; successful upstream authentication cannot self-assert Tenant/roles/permissions; invalid Tenant membership is denied; authorization code is single-use/short-lived and bound to the registered client/redirect contract; code exchange issues a Security USER token with resolved Tenant role permissions; logout/session invalidation behaves according to the implemented session contract; confidential client secrets are never exposed to browser flow; PKCE is enforced for public clients where applicable | Architecture: VSEC-SD-INT-001 v1.1. Exact upstream IdP configuration/session storage mechanism must follow the approved runtime choice and must not be invented beyond what implementation requires. |
| SEC-DEPLOY-001 | Deploy and verify Security OAuth/authentication service in Railway DEV | NOT STARTED | Railway DEV runs the verified Security build containing the approved auth/OAuth endpoints and current RBAC/token implementation | CI for the exact deployed commit is green; `/.well-known/jwks.json` returns 200; unauthenticated `POST /oauth/token` reaches the Security OAuth handler and returns the correct auth/validation response rather than 404; interactive authorization/login endpoints are routable; a controlled DEV flow obtains a real Security USER token and a service/delegated token without exposing secrets; deployment evidence is recorded | Closes Audit Core pending issue `XMOD-SEC-01`. Execute after `SEC-AUTH-001` so Security is deployed once with the complete approved authentication/OAuth boundary. |

## Completion rule

A task becomes COMPLETE only after implementation and the stated positive/negative tests pass. Creating a task/design, committing defaults or observing a route locally does not complete the implementation. Deployment tasks additionally require evidence from the exact deployed commit and live DEV endpoint.
